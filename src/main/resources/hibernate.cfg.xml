<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- PostgreSQL Driver -->
        <property name="hibernate.connection.driver_class">
            org.postgresql.Driver
        </property>

        <!-- Database URL -->
        <property name="hibernate.connection.url">
            jdbc:postgresql://localhost:5432/home_service_db
        </property>

        <!-- Username -->
        <property name="hibernate.connection.username">
            postgres
        </property>

        <!-- Password -->
        <property name="hibernate.connection.password">
            postgres
        </property>

        <!-- PostgreSQL Dialect -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.PostgreSQLDialect
        </property>

        <!-- Show SQL in console -->
        <property name="hibernate.show_sql">
            true
        </property>

        <!-- Format SQL -->
        <property name="hibernate.format_sql">
            true
        </property>

        <!-- Automatically update tables -->
        <property name="hibernate.hbm2ddl.auto">
            update
        </property>

        <!-- Entity Classes -->
        <mapping class="entity.Customer"/>
        <mapping class="entity.Specialist"/>
        <mapping class="entity.Manager"/>
        <mapping class="entity.Order"/>
        <mapping class="entity.Proposal"/>
        <mapping class="entity.Review"/>
        <mapping class="entity.Wallet"/>
        <mapping class="entity.Service"/>

    </session-factory>

</hibernate-configuration>